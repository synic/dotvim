return function(use)
	use("synic/jellybeans.vim")
	use("jnurmine/Zenburn")
	use("morhetz/gruvbox")
	use("synic/synic.vim")

	use({
		"sonph/onehalf",
		rtp = "vim",
	})

	use("gosukiwi/vim-atom-dark")
	use("tomasr/molokai")
end
